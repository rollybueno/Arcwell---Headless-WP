'use client';
import {useEffect,useRef,useState} from 'react';
export function Navigation({items}) {
 const [open,setOpen]=useState(false);const box=useRef(null);const button=useRef(null);
 useEffect(()=>{const key=e=>{if(e.key==='Escape'){setOpen(false);if(open)button.current?.focus();}};const click=e=>{if(!box.current?.contains(e.target))setOpen(false);};document.addEventListener('keydown',key);document.addEventListener('click',click);return()=>{document.removeEventListener('keydown',key);document.removeEventListener('click',click);};},[open]);
 return <div className="navigation-group" ref={box}><nav className={'nav'+(open?' open':'')} id="main-nav" aria-label="Main navigation">{items.map((item,index)=><a href={item.href} key={index} onClick={()=>setOpen(false)}>{item.label}</a>)}</nav><div className="header-actions"><a className="icon" href="/search/" aria-label="Search the journal"><svg viewBox="0 0 24 24" fill="none" aria-hidden="true"><circle cx="10.5" cy="10.5" r="6.5" stroke="currentColor" strokeWidth="1.6"/><path d="m16 16 5 5" stroke="currentColor" strokeWidth="1.6"/></svg></a><button ref={button} className="icon menu-toggle" aria-label={open?'Close navigation':'Open navigation'} aria-expanded={open} aria-controls="main-nav" onClick={()=>setOpen(!open)}><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M3 7h18M3 12h18M3 17h18" stroke="currentColor" strokeWidth="1.5"/></svg></button></div></div>;
}
export function Share(){const [status,setStatus]=useState('');return <><button className="button" onClick={async()=>{try{await navigator.clipboard.writeText(location.href);setStatus('Link copied.');}catch{setStatus('Copy the address from your browser to share this story.');}}}>Copy story link ↗</button><p className="status" role="status">{status}</p></>;}
