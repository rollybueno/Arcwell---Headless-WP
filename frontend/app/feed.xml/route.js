import {getPosts,getSite} from '../../lib/wp.mjs';
import {origin,text,publicNodes,articlePath} from '../../lib/content.mjs';
const escape=s=>String(s).replace(/[<>&"']/g,c=>({'<':'&lt;','>':'&gt;','&':'&amp;','"':'&quot;',"'":'&apos;'}[c]));
export async function GET(){const [posts,site]=await Promise.all([getPosts(),getSite()]);const body='<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>'+escape(site.generalSettings.title||'Arcwell')+'</title><link>'+escape(origin())+'</link><description>'+escape(site.arcwell.tagline||'Stories from Arcwell')+'</description>'+publicNodes(posts).map(p=>'<item><title>'+escape(text(p.title))+'</title><link>'+escape(origin()+articlePath(p))+'</link><guid>'+escape(origin()+articlePath(p))+'</guid><description>'+escape(text(p.excerpt))+'</description><pubDate>'+new Date(p.date).toUTCString()+'</pubDate></item>').join('')+'</channel></rss>';return new Response(body,{headers:{'Content-Type':'application/rss+xml; charset=utf-8'}});}
